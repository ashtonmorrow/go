node_modules
.next
out
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
.vscode
.idea

# TypeScript incremental build cache — regenerated on every `tsc` run.
tsconfig.tsbuildinfo

# Claude Code local-only settings (per-machine permission grants, etc.).
.claude/settings.local.json

# Audit output — contains live image URLs, including expired Notion S3
# signed URLs whose AWS STS temp keys (ASIA prefix, not Mike's credentials)
# trip GitHub's secret scanner. Regenerated locally by
# scripts/audit-broken-images.ts. Never commit.
scripts/_broken-images.json
